/**
 * Exit point for all the routers.
 *
 * @description An object of all of the routers.
 * @module routes/index
 */
import user from './user';
import post from './post';
import comment from './comment';

export default { user, post, comment };
